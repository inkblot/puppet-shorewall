# <%= @name %>
<%= @zone_name %><%= Array(@parent_zones).empty? ? '' : ":#{Array(@parent_zones).join(',')}" %> <%= @type %> <%= Array(@options).join(',') %> <%= Array(@in_options).join(',') %> <%= Array(@out_options).join(',') %>
